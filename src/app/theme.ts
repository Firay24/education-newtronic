"use client";
import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      light: "#5E9CF0",
      main: "#2579EB",
      dark: "#1958AE",
      contrastText: "#fff",
    },
  },
});
